services:
  - type: web
    name: nami-furniture-advisor
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --timeout 120 app:app
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
